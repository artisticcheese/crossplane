# The two Composition Functions composition.yaml's pipeline references.
# Both are maintained by crossplane-contrib and published on Crossplane's
# own registry. Versions pinned to each package's latest tagged release
# as of this demo's last verification pass.
apiVersion: pkg.crossplane.io/v1
kind: Function
metadata:
  name: function-patch-and-transform
spec:
  package: xpkg.crossplane.io/crossplane-contrib/function-patch-and-transform:v0.10.9
---
apiVersion: pkg.crossplane.io/v1
kind: Function
metadata:
  name: function-auto-ready
spec:
  package: xpkg.crossplane.io/crossplane-contrib/function-auto-ready:v0.7.0
